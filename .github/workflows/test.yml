on:
  workflow_dispatch:
jobs:
  go:
    uses: ./.github/workflows/Is_Branch.yml
    with:
      branch: 'notmain'
  check:
    runs-on: ubuntu-latest
    needs: go
    steps:
      - run: echo ${{ needs.go.outputs.branches }}
      - run: echo ${{ needs.go.outputs.branches }}
