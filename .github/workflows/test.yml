on:
  workflow_dispatch:
    inputs:
      branch:
        required: true
        type: string
jobs:
  go:
    uses: ./.github/workflows/Is_Branch.yml
    with:
      branch: ${{ inputs.branch }}
  check:
    runs-on: ubuntu-latest
    needs: go
    steps:
      - run: echo ${{ needs.go.outputs.branches }}
      - run: echo ${{ needs.go.outputs.branches }}
